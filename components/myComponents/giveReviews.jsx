import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

// Review component when pressed on any user inside ridePopUp.jsx
// Post request to database to send review + ratings
const GiveReviews = (visible,  ) => {
  return (
    <View>
      <Text>GiveReviews</Text>
    </View>
  )
}

export default GiveReviews

const styles = StyleSheet.create({})